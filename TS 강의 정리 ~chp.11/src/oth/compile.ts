// 관찰자 모드
// tsc app.ts --watch or tsc app.ts -w
// 하지만 여전히 app.ts 파일만 추적함

// 전체 프로젝트 컴파일
// tsc --init -> tsconfig.json 생성
// tsc만 입력해도 ts 파일 전체 실행

let ages: number;
age = 30;

const userNames = "Max";

console.log(userName);

const button = document.querySelector("button")!;

button.addEventListener("click", () => {
  console.log("clicked!");
});
